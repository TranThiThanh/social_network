<%= form_for comment do |f| %>
  <div class="field">
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit t("comment.update"), class: "btn btn-sm btn-primary" %>
  </div>
<% end %>
