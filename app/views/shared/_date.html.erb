<% if (Time.now - obj.created_at) < Settings.number %>
  <%= t "comment.comment" %> <%= time_ago_in_words(obj.created_at) %>
<% else %>
  <%= obj.created_at.strftime("%B %d, %Y at %l:%M:%S %P %Z") %>
<% end %>
