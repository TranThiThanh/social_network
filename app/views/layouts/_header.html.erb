<div class="main-header">
  <div class="header-holder">
    <div class="col-md-1"></div>
    <div class="col-md-11">
      <img id="icon" src="/assets/facebook.png"
        alt="<%= t "layouts.application" %>">
      <div class="header-form">
        <%= form_tag users_path, method: :get  do %>
          <%= search_field_tag :search, nil, class: "form-control",
            placeholder: t("layouts.holder") %>
          <button class="search-btn" type="submit">
            <img alt="<%= t "layouts.holder" %>" class="search-icon"
              src="/assets/search.png">
          </button>
        <% end %>
      </div>
      <div class="header-menu">
        <ul>
          <li>
            <div class="header-form" title="<%= t "layouts.profile" %>">
              <%= image_tag current_user.avatar %>
              <span><%= current_user.full_name %></span>
            </div>
          </li>
          <li><%= link_to t("layouts.home"), current_user %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <b class="glyphicon glyphicon-user"></b>
              <span class="badge">
                <%= pluralize(current_user.pending_friend_requests.count, "") %>
              </span>
            </a>
            <ul class="dropdown-menu friend">
              <% current_user.pending_friend_requests.each do |request| %>
                <li>
                  <div class="request">
                    <div class="request-image">
                      <%= link_to request.follower do %>
                        <%= image_tag request.follower.avatar %>
                      <% end %>
                    </div>
                    <div class="request-name">
                      <%= link_to request.follower do %>
                        <%= request.follower.username %>
                      <% end %>
                    </div>
                    <div class="request-buttons">
                      <%= link_to t("layouts.accept_yes"),
                        relationship_path(request.id), method: :put,
                        class: "btn btn-success" %>
                      <%= link_to t("layouts.accept_no"),
                        relationship_path(request.id),
                        method: :delete, class: "btn btn-danger" %>
                    </div>
                  </div>
                </li>
              <% end %>
            </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <b class="glyphicon glyphicon-envelope"></b>
              <span class="badge">
                0
              </span>
            </a>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <b class="glyphicon glyphicon-globe"></b>
              <span class="badge">
                <%= pluralize(current_user.pending_group.count, "") %>
              </span>
            </a>
            <ul class="dropdown-menu friend">
              <% current_user.pending_group.each do |request| %>
                <li>
                  <div class="request">
                    <div class="request-image">
                      <%= link_to request.group do %>
                        <%= image_tag request.group.avatar %>
                      <% end %>
                    </div>
                    <div class="request-name">
                      <%= link_to request.group do %>
                        <%= request.group.name_group %>
                      <% end %>
                    </div>
                    <div class="request-buttons">
                      <%= link_to t("layouts.join_yes"),
                        invite_path(request), method: :put,
                        class: "btn btn-success" %>
                      <%= link_to t("layouts.accept_no"),
                        invite_path(request),
                        method: :delete, class: "btn btn-danger" %>
                    </div>
                  </div>
                </li>
              <% end %>
            </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <b class="glyphicon glyphicon-question-sign"></b>
            </a>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <b class="glyphicon glyphicon-triangle-bottom"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("layouts.profile"), current_user %></li>
              <li>
                <%= link_to t("layouts.setting"), profile_path(current_user) %>
              </li>
              <li class="divider"></li>
              <li>
                <%= link_to t("layouts.logout"), logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>
